# Copy this file to terraform.tfvars and fill in your real values.
# terraform.tfvars is gitignored — never commit real secrets.

my_ip       = "203.0.113.25/32" # your public IP — get it with: curl -s https://ifconfig.me
db_password = "ReplaceWithYourOwnSecurePassword123!"
