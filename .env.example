POSTGRES_USER=pixeluser
POSTGRES_PASSWORD=change_me_dev_password
POSTGRES_DB=pixelshooter

# Local Docker Compose: use the "db" service name.
# AWS HW17: set to the DB EC2's private IP, e.g. DB_HOST=10.0.141.189
DB_HOST=db
DB_PORT=5432

PORT=3000
